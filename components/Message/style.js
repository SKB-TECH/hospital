import {StyleSheet} from 'react-native';
import {PADDING} from '../../outils/constants';

const styleConversation = StyleSheet.create({
  container: {
    backgroundColor: 'white',
    margin: 15,
    padding: 30,
    borderRadius: 15,
    maxWidth: '90%',
  },
});

export default styleConversation;
